plugins {
    id 'net.fabricmc.fabric-loom' version '1.17-SNAPSHOT'
}

version = '1.0'
base.archivesName = "modid"

loom {
    if (file("src/main/resources/META-INF/moreandmoreendrods.classtweaker").exists())
        accessWidenerPath = file("src/main/resources/META-INF/moreandmoreendrods.classtweaker")
}

dependencies {
    minecraft "com.mojang:minecraft:26.1.2"
    implementation "net.fabricmc:fabric-loader:0.19.3"

    // Fabric API
    implementation "net.fabricmc.fabric-api:fabric-api:0.155.2+26.1.2"
}

// We need to do this because MCreator doesn't allow to split armor textures in multiple folders
tasks.named('processResources', ProcessResources) {
    doLast {
        copy {
            from(layout.buildDirectory.dir("resources/main/assets/moreandmoreendrods/textures/entity/equipment/humanoid"))
            into(layout.buildDirectory.dir("resources/main/assets/moreandmoreendrods/textures/entity/equipment/humanoid_leggings"))
        }
    }
}

tasks.withType(JavaCompile).configureEach {
    it.options.release = 25
}

java {
    withSourcesJar()
    sourceCompatibility = JavaVersion.VERSION_25
    targetCompatibility = JavaVersion.VERSION_25
}

apply from: 'mcreator.gradle'